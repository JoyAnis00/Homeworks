// Dart is statically typed, meaning variable types are checked at compile time.
void main() {
  int number = 10; // Type is explicitly defined
  // number = "String"; // Error: A value of type 'String' can't be assigned to a variable of type 'int'.
  print(number);
}
